<Window x:Class="CalculadoraWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Calculadora Básica" Height="250" Width="350"
        Icon="Recursos/icono.ico">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Número 1 -->
        <Label Content="Número 1:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Name="txtNumero1" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <!-- Número 2 -->
        <Label Content="Número 2:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Name="txtNumero2" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <!-- Operador -->
        <Label Content="Operador (+,-,*,/):" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Name="txtOperador" Grid.Row="2" Grid.Column="1" Margin="5" TextChanged="txtOperador_TextChanged"/>

        <!-- Resultado -->
        <Label Content="Resultado:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Name="txtResultado" Grid.Row="3" Grid.Column="1" Margin="5" IsReadOnly="True"/>

        <!-- Botones -->
        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Name="btnCalcular" Content="Calcular" Width="100" Margin="5" Click="btnCalcular_Click" IsEnabled="False"/>
            <Button Name="btnLimpiar" Content="Limpiar" Width="100" Margin="5" Click="btnLimpiar_Click"/>
        </StackPanel>
    </Grid>
</Window>
